name: homassy

services:
  postgres:
    container_name: homassy-postgres
    image: postgres:16
    ports:
      - "5432:5432"
    environment:
      - POSTGRES_DB=${POSTGRES_DB}
      - POSTGRES_USER=${POSTGRES_USER}
      - POSTGRES_PASSWORD=${POSTGRES_PASSWORD}
    volumes:
      - postgres-data:/var/lib/postgresql/data
      - ./Homassy.Kratos/init-kratos-schema.sql:/docker-entrypoint-initdb.d/init-kratos-schema.sql:ro
    networks:
      - homassy-network
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER} -d ${POSTGRES_DB}"]
      interval: 10s
      timeout: 5s
      retries: 5

  homassy.migrator:
    container_name: homassy-migrator
    image: ${DOCKER_REGISTRY-}homassymigrator
    build:
      context: .
      dockerfile: Homassy.Migrator/Dockerfile
    environment:
      - ConnectionStrings__DefaultConnection=Host=${DATABASE_HOST};Port=${DATABASE_PORT};Database=${POSTGRES_DB};Username=${POSTGRES_USER};Password=${POSTGRES_PASSWORD}
    networks:
      - homassy-network
    depends_on:
      postgres:
        condition: service_healthy
    restart: "no"

  homassy.kratos:
    container_name: homassy-kratos
    image: oryd/kratos:v1.2.0
    ports:
      - "4433:4433"
      - "4434:4434"
    environment:
      # Database connection (Kratos standard env var)
      - DSN=postgres://${POSTGRES_USER}:${POSTGRES_PASSWORD}@${DATABASE_HOST}:${DATABASE_PORT}/${POSTGRES_DB}?sslmode=disable&search_path=kratos
      # Secrets (Kratos standard env vars)
      - SECRETS_COOKIE=${KRATOS_COOKIE_SECRET:-PLEASE-CHANGE-ME-I-AM-VERY-INSECURE!}
      - SECRETS_CIPHER=${KRATOS_CIPHER_SECRET:-32-character-cipher-key-here-012}
      # Email microservice
      - EMAIL_SERVICE_API_KEY=${EMAIL_SERVICE_API_KEY}
    volumes:
      - ./Homassy.Kratos/kratos.yml:/etc/config/kratos/kratos.yml:ro
      - ./Homassy.Kratos/identity.schema.json:/etc/config/kratos/identity.schema.json:ro
      - ./Homassy.Kratos/webhook_body.jsonnet:/etc/config/kratos/webhook_body.jsonnet:ro
    entrypoint: /bin/sh
    command: ["-c", "sed \"s|REPLACE_EMAIL_SERVICE_API_KEY|${EMAIL_SERVICE_API_KEY}|g\" /etc/config/kratos/kratos.yml > /tmp/kratos.yml && kratos migrate sql -e --yes -c /tmp/kratos.yml && kratos serve -c /tmp/kratos.yml --dev --watch-courier"]
    networks:
      - homassy-network
    depends_on:
      postgres:
        condition: service_healthy
      homassy.email:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "wget", "-q", "--spider", "http://localhost:4433/health/alive"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s

  homassy.api:
    container_name: homassy-api
    image: ${DOCKER_REGISTRY-}homassyapi
    build:
      context: .
      dockerfile: Homassy.API/Dockerfile
    ports:
      - "5226:8080"
    environment:
      - ASPNETCORE_ENVIRONMENT=Development
      - ASPNETCORE_HTTP_PORTS=8080
      - ASPNETCORE_URLS=http://+:8080
      - ConnectionStrings__DefaultConnection=Host=${DATABASE_HOST};Port=${DATABASE_PORT};Database=${POSTGRES_DB};Username=${POSTGRES_USER};Password=${POSTGRES_PASSWORD}
      - DATABASE_HOST=${DATABASE_HOST}
      - DATABASE_PORT=${DATABASE_PORT}
      - POSTGRES_DB=${POSTGRES_DB}
      - POSTGRES_USER=${POSTGRES_USER}
      - POSTGRES_PASSWORD=${POSTGRES_PASSWORD}
      - Kratos__PublicUrl=http://homassy-kratos:4433
      - Kratos__AdminUrl=http://homassy-kratos:4434
      - RegistrationEnabled=${REGISTRATION_ENABLED:-true}
    networks:
      - homassy-network
    depends_on:
      postgres:
        condition: service_healthy
      homassy.migrator:
        condition: service_completed_successfully
      homassy.kratos:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8080/api/v1.0/health/ready"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 30s

  homassy.web:
    container_name: homassy-web
    image: ${DOCKER_REGISTRY-}homassyweb
    build:
      context: ./Homassy.Web
      dockerfile: Dockerfile
      target: development
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=development
      - NUXT_PUBLIC_API_BASE=http://localhost:5226
      - NUXT_PUBLIC_KRATOS_URL=http://localhost:4433
    volumes:
      - ./Homassy.Web:/app
      - /app/node_modules
    networks:
      - homassy-network
    depends_on:
      postgres:
        condition: service_healthy
      homassy.api:
        condition: service_healthy
      homassy.kratos:
        condition: service_healthy

  homassy.email:
    container_name: homassy-email
    image: ${DOCKER_REGISTRY-}homassyemail
    build:
      context: .
      dockerfile: Homassy.Email/Dockerfile
    environment:
      - ASPNETCORE_ENVIRONMENT=Development
      - ASPNETCORE_URLS=http://+:8080
      - Email__SmtpServer=${EMAIL_SMTP_SERVER}
      - Email__SmtpPort=${EMAIL_SMTP_PORT}
      - Email__SenderEmail=${EMAIL_SENDER_EMAIL}
      - Email__Username=${EMAIL_USERNAME}
      - Email__Password=${EMAIL_PASSWORD}
      - Email__ApiKey=${EMAIL_SERVICE_API_KEY}
    networks:
      - homassy-network
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8080/health/live"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s

networks:
  homassy-network:
    driver: bridge

volumes:
  postgres-data:
    driver: local
